import { defineConfig } from 'astro/config';

// Update `site` to your real domain before deploying (used for canonical URLs, sitemaps, etc.)
export default defineConfig({
  site: 'https://your-cafe.example',
});
